# Generated SSH Remote helper artifacts

`wails3 task common:build:remote-helper-artifacts` generates four immutable POSIX helper binaries and `manifest.json` in this directory before desktop packaging. Generated binaries and the manifest are ignored by Git; this file only keeps the embed/build directory present in a clean checkout.
